# Each directory specified in `LIBRARY_DIRS` will be prefixed with `-L`
LIBRARY_DIRS 	= # /opt/private/lib64 ../../../project1/lib
# Each direcorty specified in `INCLUDE_DIRS` will be prefixed with `-I`
INCLUDE_DIRS 	= # /opt/include ../../../porject1/include
# Each library specified in `LIBRARIES` will be linked with
LIBRARIES 	= # ../example.a

TARGET_BIN = xi_datastream_get

include ../Makefile.helper
